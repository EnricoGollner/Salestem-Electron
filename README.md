# SitemaVendas-Electron

Para Instalar todas as dependências do package.json:
npm install - Após o git clone

npm run start - para iniciar o electron (também)

# Lembrando: Não dá push ou merge com o db.json criado
